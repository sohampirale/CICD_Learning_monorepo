name: Production Deploy Test

on:
  push:
    branches:
      - production   

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Echo message
        run: echo "🚀 This is a production deployment pipeline running!"
